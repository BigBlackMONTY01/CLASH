Fix 1 — Email + Password Auth
Implement full email + password authentication using bcrypt for password hashing and JWT for sessions. Do NOT use any third-party auth provider.
	•	Add a users table to the Drizzle schema with: id, email, password_hash, username, created_at
	•	Run the migration on Neon
	•	Create POST /api/auth/register — accepts email, password, username, validates input, hashes password with bcrypt (salt rounds 12), stores user, returns JWT
	•	Create POST /api/auth/login — accepts email, password, verifies against hash, returns JWT
	•	JWT secret comes from env var JWT_SECRET. Token expires in 7 days
	•	Create auth middleware that reads Bearer token from Authorization header and attaches user to req.user
	•	On the frontend, store JWT in localStorage. On login/register success, save token and user info
	•	The Sign In button in the nav opens a modal with two tabs: Sign In and Register. Both have email + password fields. Register also has a username field
	•	After successful login, hide the Sign In button from the nav entirely. Show only the profile button with the user’s username
	•	On logout (from the profile overlay), clear localStorage and reset to logged-out state
Fix 2 — 1v1 HTTP 500 Error
The 1v1 mode is returning a 500 error. Debug and fix the 1v1 route handler completely. Make sure:
	•	The route exists and is registered in the Express app
	•	Error handling is proper with try/catch and meaningful error messages
	•	The AI judge is properly initialized and called
	•	CORS is not blocking it from the Vercel frontend
Fix 3 — Nav UI Cleanup
Current nav has: CLASH logo + BETA badge, Sign In button, mute button, light mode button, profile button — all crammed together and too cluttered.
Redesign the nav so:
	•	When logged OUT: show Sign In button (smaller, less prominent), mute icon, light/dark toggle icon, no profile button
	•	When logged IN: hide Sign In button completely, show mute icon, light/dark toggle icon, and a compact profile button showing just the avatar icon + username
	•	Tapping the profile button opens a glass overlay panel (not a full page) — frosted glass effect, backdrop-filter: blur(20px), semi-transparent dark background — showing: username, email, rank, total debates, win rate, and a Logout button at the bottom
	•	The mute and light mode buttons should be smaller and more compact — icon only, no border box, subtle on hover
Fix 4 — 1V1 Button Style on Home Screen
The 1V1 button on the home/play screen has a blue tint and left-aligned text with a chevron arrow — it looks completely different from the other buttons (VS AI, LEADERBOARD, GAUNTLET MODE). Fix it to match: centered text, bold caps, consistent dark background, no chevron.
Fix 5 — START CLASH Button Positioning
On the match setup screen, the START CLASH button is misaligned. It should be a full-width primary CTA button, centered, below the FOR/AGAINST selection, clearly dominant over the BACK button.