CLASH — Full Rebuild. Do everything exactly as specified. Do not skip anything.
CONTEXT:
Monorepo. Frontend: artifacts/clash/src/. Backend: artifacts/api-server/src/. DB: @workspace/db at lib/db/src/. Backend on Render at clash-1xpu.onrender.com. Frontend on Vercel at clashbant.vercel.app. Auth routes: artifacts/api-server/src/routes/auth.ts. Main Express app: artifacts/api-server/src/app.ts.

FIX 1 — REMOVE LIGHT MODE COMPLETELY
	1.	In artifacts/clash/src/index.css delete the entire :root light mode block. Keep only .dark. Move all .dark variables directly onto :root so dark is always active. Replace every CSS variable placeholder with real values:

--background: 0 0% 5%;
--foreground: 0 0% 95%;
--border: 0 0% 15%;
--card: 0 0% 8%;
--card-foreground: 0 0% 95%;
--card-border: 0 0% 15%;
--popover: 0 0% 8%;
--popover-foreground: 0 0% 95%;
--popover-border: 0 0% 15%;
--primary: 0 65% 47%;
--primary-foreground: 0 0% 100%;
--secondary: 0 0% 12%;
--secondary-foreground: 0 0% 85%;
--muted: 0 0% 12%;
--muted-foreground: 0 0% 55%;
--accent: 0 0% 15%;
--accent-foreground: 0 0% 95%;
--destructive: 0 65% 47%;
--destructive-foreground: 0 0% 100%;
--input: 0 0% 20%;
--ring: 0 65% 47%;
--sidebar: 0 0% 6%;
--sidebar-foreground: 0 0% 90%;
--sidebar-border: 0 0% 15%;
--sidebar-primary: 0 65% 47%;
--sidebar-primary-foreground: 0 0% 100%;
--sidebar-accent: 0 0% 12%;
--sidebar-accent-foreground: 0 0% 90%;
--sidebar-ring: 0 65% 47%;
--chart-1: 0 65% 47%;
--chart-2: 210 65% 55%;
--chart-3: 142 55% 45%;
--chart-4: 45 90% 55%;
--chart-5: 280 55% 55%;


	2.	In artifacts/clash/src/main.tsx or App.tsx, force dark mode permanently on load:

document.documentElement.classList.add('dark');


	3.	Remove the light/dark toggle button from the nav entirely. Remove all theme toggle logic, localStorage theme preference code, and any liquid glass CSS added for light mode.

FIX 2 — NAV REDESIGN
Find the nav component (search for “Sign In” in artifacts/clash/src/):
	•	Remove the light/dark toggle button completely
	•	Mute button: icon only (🔊 / 🔇), no border box, no background, 36x36px tap target, opacity 0.5 on hover. Compact, no box around it.
	•	When logged OUT: show compact mute icon + Sign In button (small, red outline)
	•	When logged IN (clash_token exists in localStorage): hide Sign In completely. Show compact mute icon + profile pill (avatar circle with first letter + username)
	•	Tapping profile pill opens a floating overlay panel over the current screen — NOT a new page:
	•	position: fixed, top-right below nav
	•	backdrop-filter: blur(40px)
	•	background: rgba(10,10,10,0.85)
	•	border: 1px solid rgba(255,255,255,0.1)
	•	border-radius: 20px
	•	Contents: avatar circle, username (bold white), email (small muted), rank badge, total debates, win rate, Logout button at bottom (red text)
	•	Tapping outside closes it

FIX 3 — AUTH: LOGIN + REGISTER
	1.	Open artifacts/api-server/src/routes/index.ts. Confirm authRouter is imported from ./auth and mounted. If missing:

import authRouter from "./auth";
router.use(authRouter);


	2.	Check lib/db/src/ schema. Confirm users table exists with id, email, passwordHash, playerId, createdAt. If missing, create it and run pnpm drizzle-kit push.
	3.	Confirm bcryptjs and jsonwebtoken are in artifacts/api-server/package.json. If not, add and run pnpm install.
	4.	Confirm JWT_SECRET is read from process.env.JWT_SECRET.
	5.	Frontend Sign In modal must call POST /api/auth/register and POST /api/auth/login with Content-Type: application/json using the Vercel proxy (/api/auth/login not the full Render URL). On success:

localStorage.setItem('clash_token', token);
localStorage.setItem('clash_user', JSON.stringify({ userId, playerId, email, username }));


	6.	After login/register, update React state immediately. Hide Sign In, show profile pill.

FIX 4 — LOGOUT CLEARS ALL STATE
On logout:

Object.keys(localStorage).filter(k => k.startsWith('clash_')).forEach(k => localStorage.removeItem(k));
setUser(null);
setPlayer(null);
setProfile(null);
setStats(null);


On login/register success, fully replace all state — never merge with old data. Stats, username, rank must come fresh from the server for the new user.

FIX 5 — STATS TIED TO EMAIL NOT DEVICE
When logged in, all stats (debates, wins, rank) must be tied to the email/userId from the JWT — not deviceId. If they log out and log in with a different email, they get that email’s stats. Fix the player profile fetch to use userId from JWT when logged in, falling back to deviceId only when not logged in.

FIX 6 — BUILD 1V1 BACKEND
If artifacts/api-server/src/routes/1v1.ts doesn’t exist, create it:

import { Router } from "express";
import { nanoid } from "nanoid";

const router = Router();

const rooms: Record<string, {
  code: string;
  topic: string;
  creatorId: string;
  joinerId?: string;
  status: "waiting" | "active" | "done";
  createdAt: number;
}> = {};

router.post("/1v1/create", (req, res) => {
  try {
    const { topic, playerId } = req.body;
    if (!topic || !playerId) { res.status(400).json({ error: "topic and playerId required" }); return; }
    const code = nanoid(6).toUpperCase();
    rooms[code] = { code, topic, creatorId: String(playerId), status: "waiting", createdAt: Date.now() };
    res.json({ code, topic, status: "waiting" });
  } catch (err) {
    console.error("1v1/create error:", err);
    res.status(500).json({ error: "Failed to create room" });
  }
});

router.post("/1v1/join", (req, res) => {
  try {
    const { code, playerId } = req.body;
    if (!code || !playerId) { res.status(400).json({ error: "code and playerId required" }); return; }
    const room = rooms[code.toUpperCase()];
    if (!room) { res.status(404).json({ error: "Room not found." }); return; }
    if (room.status !== "waiting") { res.status(409).json({ error: "Room already in progress." }); return; }
    if (room.creatorId === String(playerId)) { res.status(409).json({ error: "Can't join your own room." }); return; }
    room.joinerId = String(playerId);
    room.status = "active";
    res.json({ code: room.code, topic: room.topic, status: room.status });
  } catch (err) {
    console.error("1v1/join error:", err);
    res.status(500).json({ error: "Failed to join room" });
  }
});

router.get("/1v1/room/:code", (req, res) => {
  try {
    const room = rooms[req.params.code.toUpperCase()];
    if (!room) { res.status(404).json({ error: "Room not found" }); return; }
    res.json(room);
  } catch (err) {
    res.status(500).json({ error: "Failed to get room" });
  }
});

export default router;


In artifacts/api-server/src/routes/index.ts add:

import oneVOneRouter from "./1v1";
router.use(oneVOneRouter);


Run pnpm add nanoid inside artifacts/api-server/.
Frontend: CREATE ROOM calls POST /api/1v1/create with { topic, playerId }. JOIN ROOM calls POST /api/1v1/join with { code, playerId }.
1V1 topic section: reduce to 3 preset topics only. Add a custom input field where the host can type their own topic. Round options: 1, 2, 3, 4, 5, 7, 12, 15.

FIX 7 — 1V1 BUTTON ON HOME SCREEN
Find the 1V1 button on the home screen:
	•	Text: ⚔️ 1V1 — nothing else
	•	Keep the blue tint background
	•	Remove any glow or bloom on the blue tint
	•	Add an animated dark blue laser border that travels clockwise around the button perimeter continuously, leaving a fading light trail behind it like a comet. Implement using a CSS @keyframes pseudo-element rotating around the border. The laser color is #1a3aff, trail fades to transparent behind the moving point. No glow on the fill, only on the border animation.

FIX 8 — START CLASH BUTTON
On the match setup screen:
	•	START CLASH: full width, centered, below FOR/AGAINST cards, background: #C0392B, white bold text, large and dominant
	•	BACK button: smaller, secondary style, not competing

FIX 9 — AI RESPONSE TONE
Find the system prompt sent to the Claude API in the backend. If it doesn’t already have these instructions, add them:
	•	Max 2 to 4 sentences per response. Never use bullet points or lists.
	•	Write like a real human debater — punchy, direct, conversational
	•	Rude and mocking when the opponent is weak, but only when it fits naturally
	•	Zero AI phrases: no “certainly”, “absolutely”, “great point”, “I understand your perspective”
	•	Call out weak arguments bluntly
	•	Sound like someone with real opinions, not an assistant
	•	If these instructions are already there, leave them alone.

FIX 10 — AFTER MATCH RESULT SHOWS “INVALID BODY”
Find the result/scoring API call that fires after a match ends. Fix the request body to be valid JSON with all required fields included. Make sure the response correctly displays scores, rank, and result card. Do not leave the user on a broken screen after a match.

FIX 11 — 404 ON AUTH AND 1V1 ROUTES
Auth and 1V1 routes are returning 404 meaning they’re not registered. Check artifacts/api-server/src/routes/index.ts — confirm both authRouter and oneVOneRouter are imported and mounted. Check vercel.json to confirm /api/(.*) is proxied to the Render backend. If the rewrite rule is missing add it:

{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://clash-1xpu.onrender.com/api/$1" }
  ]
}


DO NOT BREAK: VS AI debate flow, scoring, leaderboard, activity feed, gauntlet mode, share card, replay system. Run pnpm build at the end and fix any TypeScript errors before finishing.​​​​​​​​​​​​​​​​